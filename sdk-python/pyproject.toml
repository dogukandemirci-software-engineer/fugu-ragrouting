[project]
name = "fugu-sdk"
version = "0.1.0"
description = "Python SDK for the FUGU routed RAG API"
requires-python = ">=3.9"
dependencies = ["requests>=2.31"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
